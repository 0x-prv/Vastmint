const { ethers, RITUAL_WALLET, walletAbi, TEE_SERVICE_REGISTRY, HTTP_CAPABILITY, registryAbi } = require('./_config');
async function main(){ const [signer]=await ethers.getSigners(); const user=process.env.RITUAL_WALLET_USER || signer.address; const wallet=await ethers.getContractAt(walletAbi,RITUAL_WALLET); const block=await ethers.provider.getBlockNumber(); const registry=await ethers.getContractAt(registryAbi,TEE_SERVICE_REGISTRY); const services=await registry.getServicesByCapability(HTTP_CAPABILITY,true); console.log(JSON.stringify({user,currentBlock:block,balance:(await wallet.balanceOf(user)).toString(),lockUntil:(await wallet.lockUntilOf(user)).toString(),requiredTtl:process.env.RITUAL_TTL || '100',executorAvailable:services.length>0},null,2)); }
main().catch((e)=>{ console.error(e); process.exit(1); });
